# 2026-08-09 — 对标 Float 重构 agent 引导与文档体系

> 本次为流程/文档基建，不含功能代码。基准项目：Float（`I:\dev\Float`）。

## 背景

审查发现 agent 引导文档（CLAUDE.md / AGENTS.md）严重脱节：描述已删除的 BLE 子系统、错误的导航实现与测试设备（见 postmortem [001](../postmortem/001-doc-vs-code-drift.md)）。决定完全对标 Float 的流程与规范重构。

## 改动

### CLAUDE.md 真实化

- 符号链接（mode `120000` → `100644`）转为真实普通文件
- 内容重写为真实现状：USB/MTP 唯一数据通路、Navigation 3、Metro DI、Coil 3、Khronicle、ktfmt + detekt
- 结构对标 Float：Project Context / Essential Commands / Structure & Key Patterns / Conventions + 🔴 强制规范 / Current State / Git Hooks
- 强制规范纳入：先写 commit message、@Preview、本地跑 detekt+ktfmtCheck、开写前读 postmortem

### AGENTS.md 弃用

- 内容替换为指向 CLAUDE.md 的指针（保留文件以兼容 Cursor/Copilot 等读 AGENTS.md 的工具）

### docs 体系搭建（对标 Float）

- `docs/README.md` — 文档索引
- `docs/engineering/git-workflow.md` — 分支模型 / Conventional Commits / commit 纪律 / 审查清单 / 发布
- `docs/engineering/code-style.md` — Kotlin/Compose 规范 / 状态与协程 / 测试 / detekt
- `docs/postmortem/` — README 索引 + TEMPLATE + `001-doc-vs-code-drift`
- `docs/development-log/` — README + 本篇
- 归档 7 份已完成/过时规划文档 → `docs/archive/`；`TODO.md` → `docs/planning/todo.md`

### 机器执行约束

- 新增 `.githooks/pre-push`（detekt + ktfmtCheck + lint + test + assembleDebug）
- CI `ci.yml` 增加 detekt 步骤（此前 detekt.yml 已配置但 CI 未跑）；detekt 存量 44 条违规以 `detekt-baseline.xml` 吸收
- README / CONTRIBUTING 清理 BLE 残留引用与测试设备矛盾

## 提交

- `0251bf3` `docs: realign agent guidance and docs system to Float`
- `cd756a8` `chore: enable detekt gate with baseline and add pre-push hook`

## 待办

- [x] `./gradlew detekt` 全量通过（baseline 吸收存量 44 条，新违规仍会失败）
- [ ] 真机回归：Nikon Z30 连接验证 MTP 同步链路不受影响（本次未改功能代码，风险低）
- [x] 修复 CI `ktfmtCheck` 红色门禁（`bcb7bee` 引入，commit `e873861`，见 `docs/planning/todo.md` P0）

---

## P0 修复：ktfmtFormat（同日续）

`bcb7bee perf(usb)` 引入的 9 个未格式化文件已用 `./gradlew ktfmtFormat` 修复，单独提交 `e873861 style: apply ktfmtFormat`。

- 仅格式改动，无逻辑变化（逐文件抽查 diff + `compileDebugKotlin` 验证）
- 副作用处理：
  - ktfmt 清理 10 条未使用 import → 对应 detekt-baseline 条目移除（baseline 条目 34 → 24）
  - `LocalPhotosViewModel.baseDir` 因 ktfmt 将 `.absolutePath` 移至下一行，baseline 签名失配 → 手动更新签名文本
  - `detektBaselineMain` 无法运行（网络 TLS 拉取 compose 依赖失败），改手动维护 baseline
- 本地 gate 验证：`ktfmtCheck` ✅ / `detekt` ✅ / `compileDebugKotlin` ✅

---

## Apple 视角设计评审 & 行动计划（同日续）

通读代码库与活动文档后，以 Apple 产品/研发哲学对 v2.3 做全项目评审，发现「0 已知问题」标签不成立。

### 发现（详见 `docs/review/2026-08-09-design-review.md`）

| # | 发现 | 严重度 |
|---|---|---|
| R1 | 双 MTP 管线：前台 UI 与后台自动同步各持一个 `MtpDevice`（`GalleryViewModel.kt:142` / `UsbSyncService.kt:60`） | 🔴 |
| R2 | 去重键不一致：UI 硬编码 `storageId=0`，后台用真实 storageId（`GalleryViewModel.kt` 6 处） | 🔴 |
| R3 | `PhotoSyncManager.kt:10` 假注释：`clearAll`/`clearStorage` 零调用 | 🟠 |
| R4 | MediaStore 路径硬编码 "Nikon Z30"（`GalleryViewModel.kt:1034` / `UsbSyncCoordinator.kt:156`） | 🟠 |
| R5 | 功能膨胀 + PRD P4 冷启动痛点未闭环（`MainActivity.kt:136` onboarding 是空 stub） | 🟠 |
| R6 | 测试赤字（仅 2 单测）+ `GalleryViewModel` 1099 行 / `GalleryScreen` 1913 行 | 🟡 |

### 产出

- 新增 `docs/review/`（索引 + 设计评审）
- 新增 `docs/planning/action-plan.md`：P0 止血（去重/管线/路径/剪枝）→ P1 核心路径 → P2 工程债 → P3 收尾，每项预写 commit message
- `docs/planning/todo.md`：旧 P0–P3 折叠归档指向 action-plan；「已知问题」表更正为 4 项 P0 正确性缺陷（撤回"0 已知问题"表述）
- `docs/README.md` 索引补充评审与行动计划

### 待办

- [ ] action-plan P0-1 ~ P0-4 逐个原子 commit 落地（每 commit 预写 message，本地跑 gate）
- [ ] P1 核心路径（冷启动引导 / 新照片默认主路径 / 传输回看）
- [ ] P2 工程债（拆 God Object / 补核心单测 / detekt 归零）
- [ ] P3 运营收尾（测试设备统一 / 推送验证 CI / 真机回归）

---

## P0 止血实施记录（同日续）

action-plan P0 四项已全部落地（gate 本地验证：ktfmt + detekt + compileDebugKotlin 绿）。

| 项 | commit | 说明 |
|---|---|---|
| P0-1 | `2961280` | 保存路径用 `cameraInfo.model`，strings.xml 设备名通用化 |
| P0-2 | `b75e87b` | `PhotoInfo` 加 `storageId`，去重键双管线一致（清 1 条 baseline） |
| P0-3 | `9344686` | MTP 会话护栏（`hasActiveSession`）；阶段2 共享实例降级至 P2-1 |
| P0-4 | `220aa12` | dedup 改软校验（`name:size` 身份），替代假"自动剪枝"（删除 `DedupKey` 死代码） |

### 实施中新发现：自动同步未接线（R7）

- `UsbSyncService.createStartIntent` / `ACTION_SYNC` **零调用**——后台自动同步从未真正触发
- `SettingsScreen` 的 auto-sync 开关写 `prefs.autoSyncEnabled`，但该值**无任何消费者**——开关无效
- `todo.md` 此前宣称"后台自动同步 ✅"与代码不符（评审 R5 同族）
- 处置：列入 action-plan **P1-4**（产品决策：接线自动同步 或 移除死代码）

### 实施中发现：既有测试失败

- `testDebugUnitTest` 有 **6 个 pre-existing 失败**（`LocalPhotosViewModelTest` 环境相关，纯 JVM 跑挂；stash 改动后同样失败，确认与 P0 无关）
- 处置：列入 P2-2 补核心单测时一并处理

### 待办（更新）

- [x] action-plan P0-1 ~ P0-4
- [ ] P1 核心路径（P1-1 引导 / P1-2 默认主路径 / P1-3 传输回看 / P1-4 自动同步决策）
- [ ] P2 工程债（拆 God Object / 补核心单测含修既有失败 / detekt 归零）
- [ ] P3 运营收尾（测试设备统一 / 推送验证 CI / 真机回归）

---

## P1 核心路径实施记录（同日续）

action-plan P1 四项全部落地（gate 本地验证：ktfmt + detekt + compileDebugKotlin 每 commit 绿）。

| 项 | commit | 说明 |
|---|---|---|
| P1-1 | `61fc9a7` | 冷启动一屏引导：`FirstRunGuideScreen` + `NavRoute.FirstRunGuide`，`prefs.guideSeen` 持久化；设置页「使用说明」落地空 stub（消 baseline `UnusedParameter`） |
| P1-2 | `97f7c8e` | 新照片默认主路径：`filterMode` 默认 `NEW`；主 CTA「传输全部新照片 (N)」；分组/排序/网格收进溢出菜单；downloadFormat 与默认视图解耦 |
| P1-3 | `f6d2f9b` | 传输完成回看：完成面板新增「本次传输清单」（缩略图 + MediaStore 名称，点击系统相册定位） |
| P1-4 | `6a1c331` | 移除未接线自动同步死代码（YAGNI）：`UsbSyncService`/`UsbSyncCoordinator`/无效开关/通知 channel/Manifest 权限/`hasActiveSession` 护栏/死字符串/baseline |

### 实施要点

- **P1-2 解耦决策**：`downloadFormat`（全部/仅 JPEG/仅 RAW）原先同时驱动「默认滤镜」（`downloadFormatToFilter`）。P1-2 后默认滤镜恒为 `NEW`，downloadFormat 仅控制传输选择（`handlesForFormat`），设置页描述同步改为「传输时包含的照片格式」。避免两个概念互相打架。
- **`hasActiveSession` 护栏随 P1-4 移除**：P0-3 护栏的唯一读方是 `UsbSyncCoordinator`；移除后台管线后成 write-only，单管线下并发风险不复存在，删之（YAGNI，非回退 P0-3——P0-3 的"单 MtpDevice open"由单管线构造保证）。

### 待办（更新）

- [x] action-plan P0-1 ~ P0-4
- [x] action-plan P1-1 ~ P1-4
- [ ] P2 工程债（拆 God Object / 补核心单测含修既有失败 / detekt 归零）
- [ ] P3 运营收尾（测试设备统一 / 推送验证 CI / 真机回归）

---

## P2 工程债实施记录（同日续）

> 文档先行：先记录 commit 计划，再动代码。已核对现状：`GalleryViewModel.kt` 1105 行、`GalleryScreen.kt` 2152 行、detekt baseline **17 条**（非 action-plan 原估 24，P0/P1 已清部分）、`testDebugUnitTest` **6 个既有失败**。

### 现状核对

- **6 个既有失败根因**（全部 `LocalPhotosViewModelTest`，纯 JVM 无 Robolectric）：
  1. `Uri.parse` / `MatrixCursor.addRow` — mockable.jar「not mocked」RuntimeException
  2. `Uri.EMPTY` — mockable.jar 字段为 null → NPE「EMPTY must not be null」
  3. `goBack from folder` — **真 bug**：`LocalPhotosViewModel.scope = CoroutineScope(Dispatchers.IO + …)` 硬编码，未用注入的 `ioDispatcher`（违反 CLAUDE.md「Dispatcher 注入」），测试 `advanceUntilIdle()` 等不到真实 IO 线程
  4. `loadRoot queries MediaStore` 的 query mock 用 `firstArg<String>()` 比对 Uri（ClassCast 隐患），需按 selection 参数重写
- **detekt baseline 17 条分布**：`GalleryViewModel` 4（LargeClass / SwallowedException / TooGenericExceptionCaught / UnusedPrivateProperty `gen`）、`GalleryScreen` 3（ComplexCondition×2 / UnusedPrivateProperty `totalSelected`）、`LocalPhotosViewModel` 2（TooGenericExceptionCaught / UnusedPrivateProperty `baseDir` 死代码）、`NikonUsbManager` 3（FunctionOnlyReturningConstant `getBatteryLevel` 死桩 / TooGenericExceptionCaught / UnusedParameter `mtpDevice`）、`LocalPhotosViewModelTest` 1（InvalidPackageDeclaration，包名错）、`TransferHistoryScreen` 1（MatchingDeclarationName `TransferRecord`）、`PhotoDetailSheet` 1、`MetroAppComponentFactory` 1（ExpressionBodySyntax）

### Commit 计划（先写 message 再写代码）

| # | commit message | 内容 |
|---|---|---|
| P2-1a | `refactor(usb): extract GalleryStateMachine from GalleryViewModel` | sealed state + 筛选/排序/分组/选择纯逻辑 |
| P2-1b | `refactor(usb): extract ThumbnailProvider from GalleryViewModel` | 四类缓存 + EXIF 方向 |
| P2-1c | `refactor(usb): extract TransferEngine from GalleryViewModel` | 传输编排 + MediaStore 保存 |
| P2-1d | `refactor(usb): extract ConnectionManager from GalleryViewModel` | USB 生命周期 + 浏览/枚举 |
| P2-2a | `fix(usb): use injected dispatcher in LocalPhotosViewModel scope` | 修既有失败根因 |
| P2-2b | `test(usb): fix LocalPhotosViewModel tests for plain JVM` | 静态 mock + mockk Cursor + 删 baseDir + 修 package |
| P2-2c | `test(usb): add PhotoSyncManager dedup tests` | 构造注入 SharedPreferences + 内存 fake |
| P2-2d | `test(usb): add GalleryStateMachine transition tests` | 状态迁移 + 筛选/排序/选择 |
| P2-2e | `test(usb): add TransferEngine failure tests` | 失败重试/取消/MediaStore 失败 |
| P2-3 | `chore: repay detekt baseline debt` | 17 → 0（含删 getBatteryLevel 死桩） |

### 待办（更新）

- [x] action-plan P0-1 ~ P0-4
- [x] action-plan P1-1 ~ P1-4
- [ ] P2-1a ~ P2-1d（拆 God Object，每 commit 本地 gate）
- [ ] P2-2a ~ P2-2e（修既有失败 + 补核心单测）
- [ ] P2-3（detekt baseline 17 → 0）
- [ ] P3 运营收尾
