# 2026-08-09 — 对标 Float 重构 agent 引导与文档体系

> 本次为流程/文档基建，不含功能代码。基准项目：Float（`I:\dev\Float`）。

## 背景

审查发现 agent 引导文档（CLAUDE.md / AGENTS.md）严重脱节：描述已删除的 BLE 子系统、错误的导航实现与测试设备（见 postmortem [001](../postmortem/001-doc-vs-code-drift.md)）。决定完全对标 Float 的流程与规范重构。

## 改动

### CLAUDE.md 真实化

- 符号链接（mode `120000` → `100644`）转为真实普通文件
- 内容重写为真实现状：USB/MTP 唯一数据通路、Navigation 3、Metro DI、Coil 3、Khronicle、ktfmt + detekt
- 结构对标 Float：Project Context / Essential Commands / Structure & Key Patterns / Conventions + 🔴 强制规范 / Current State / Git Hooks
- 强制规范纳入：先写 commit message、@Preview、本地跑 detekt+ktfmtCheck、开写前读 postmortem

### AGENTS.md 弃用

- 内容替换为指向 CLAUDE.md 的指针（保留文件以兼容 Cursor/Copilot 等读 AGENTS.md 的工具）

### docs 体系搭建（对标 Float）

- `docs/README.md` — 文档索引
- `docs/engineering/git-workflow.md` — 分支模型 / Conventional Commits / commit 纪律 / 审查清单 / 发布
- `docs/engineering/code-style.md` — Kotlin/Compose 规范 / 状态与协程 / 测试 / detekt
- `docs/postmortem/` — README 索引 + TEMPLATE + `001-doc-vs-code-drift`
- `docs/development-log/` — README + 本篇
- 归档 7 份已完成/过时规划文档 → `docs/archive/`；`TODO.md` → `docs/planning/todo.md`

### 机器执行约束

- 新增 `.githooks/pre-push`（detekt + ktfmtCheck + lint + test + assembleDebug）
- CI `ci.yml` 增加 detekt 步骤（此前 detekt.yml 已配置但 CI 未跑）；detekt 存量 44 条违规以 `detekt-baseline.xml` 吸收
- README / CONTRIBUTING 清理 BLE 残留引用与测试设备矛盾

## 提交

- `0251bf3` `docs: realign agent guidance and docs system to Float`
- `cd756a8` `chore: enable detekt gate with baseline and add pre-push hook`

## 待办

- [x] `./gradlew detekt` 全量通过（baseline 吸收存量 44 条，新违规仍会失败）
- [ ] 真机回归：Nikon Z30 连接验证 MTP 同步链路不受影响（本次未改功能代码，风险低）
- [x] 修复 CI `ktfmtCheck` 红色门禁（`bcb7bee` 引入，commit `e873861`，见 `docs/planning/todo.md` P0）

---

## P0 修复：ktfmtFormat（同日续）

`bcb7bee perf(usb)` 引入的 9 个未格式化文件已用 `./gradlew ktfmtFormat` 修复，单独提交 `e873861 style: apply ktfmtFormat`。

- 仅格式改动，无逻辑变化（逐文件抽查 diff + `compileDebugKotlin` 验证）
- 副作用处理：
  - ktfmt 清理 10 条未使用 import → 对应 detekt-baseline 条目移除（baseline 条目 34 → 24）
  - `LocalPhotosViewModel.baseDir` 因 ktfmt 将 `.absolutePath` 移至下一行，baseline 签名失配 → 手动更新签名文本
  - `detektBaselineMain` 无法运行（网络 TLS 拉取 compose 依赖失败），改手动维护 baseline
- 本地 gate 验证：`ktfmtCheck` ✅ / `detekt` ✅ / `compileDebugKotlin` ✅
